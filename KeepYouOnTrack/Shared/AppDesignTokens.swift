import SwiftUI

enum AppDesignTokens {
    static let accentRed = Color.accentColor
    static let accent = Color.accentColor
    static let backgroundBlack = Color(red: 0.03, green: 0.03, blue: 0.035)
    static let backgroundBlackSoft = Color(red: 0.05, green: 0.05, blue: 0.06)
    static let backgroundBlackDeep = Color(red: 0.02, green: 0.02, blue: 0.025)
}
